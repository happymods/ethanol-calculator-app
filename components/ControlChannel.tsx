import { View, StyleSheet } from "react-native";

export default function ControlChannel() {
    return(
        <View style={styles.controlChannelContainer}>

        </View>
    );
}

const styles = StyleSheet.create({
    controlChannelContainer: {},
});